#include "main.h"
/**
 * add()- Adds two given integers
 * @a: first integer
 * @b: second integer
 * Return: Returns the final output
 */
int add(int a, int b)
{
	int result;

	result = (a + b);

	return (result);
}
