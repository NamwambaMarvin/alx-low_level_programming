#include "main.h"
#include <stdlib.h>
/**
 *_abs - function to get absolute value of integer
 *@a: Integer parsed
 *Return: final value returbed by the function
 */
int _abs(int a)
{
	int absolute = abs(a);

	return (absolute);
}
