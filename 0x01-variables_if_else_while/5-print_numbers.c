#include <stdio.h>
/**
 * main - entry point of the program
 * Return: Returns final value of thr function
 */
int main(void)
{
	int node;

	for (node = 0; node < 10; ++node)
	{
		printf("%d", node);
	};
	printf("\n");
	return (0);
}
