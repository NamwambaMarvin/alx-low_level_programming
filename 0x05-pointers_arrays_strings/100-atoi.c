#include "main.h"
#include <stdlib.h>
/**
 * _atoi - Convert string into integer
 * Return integer
 * @s: - String to be converted
 */
int _atoi(char *s)
{
	int number = atoi(s);

	return (number);
}
