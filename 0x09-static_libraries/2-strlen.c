#include "main.h"
#include <string.h>
/**
 * _strlen - Caltulates the length of a string
 * @s: String whose length is to be calculated
 * Return: Legth of the string
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
