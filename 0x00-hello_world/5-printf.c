#include <stdio.h>
/**
 * main - This is the entry point of our program
 * printf - Prints value that we want
 * Return: Returns a zero as told
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
